doctype 5

html
  head
    title All files available to read
    include bootstrap
  body.container
    h1.text-center #{infos.length} files available:
    - foreach(a ; infos)
      p Have filename: #{ a.name }
        //- |= a.name
      
